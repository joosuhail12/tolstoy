---
title: "Execute Action"
openapi: "POST /actions/execute"
---

# Execute Action

Execute a single action independently outside of a flow context. This endpoint is perfect for testing actions, one-off operations, or building custom integrations that don't require full workflow orchestration.

<RequestExample>

```bash cURL
curl -X POST "https://tolstoy.getpullse.com/actions/execute" \
  -H "x-org-id: org_1234567890abcdef" \
  -H "x-user-id: user_abcdef1234567890" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "email-send",
    "inputs": {
      "to": "customer@example.com",
      "subject": "Welcome to our platform!",
      "body": "Thanks for signing up. We are excited to have you on board!",
      "template": "welcome-template"
    },
    "timeout_seconds": 60,
    "tags": ["manual-execution", "customer-onboarding"]
  }'
```

```typescript TypeScript
interface ExecuteActionRequest {
  action: string;
  inputs: Record<string, any>;
  timeout_seconds?: number;
  tags?: string[];
  dry_run?: boolean;
  priority?: 'low' | 'normal' | 'high';
}

interface ExecuteActionResponse {
  success: boolean;
  data: {
    execution_id: string;
    action: string;
    status: 'completed' | 'failed' | 'timeout';
    inputs: Record<string, any>;
    outputs?: Record<string, any>;
    error?: {
      code: string;
      message: string;
      details?: Record<string, any>;
    };
    performance: {
      duration_ms: number;
      memory_used_mb?: number;
    };
    executed_at: string;
  };
}

const executeAction = async (request: ExecuteActionRequest): Promise<ExecuteActionResponse> => {
  const response = await fetch('https://tolstoy.getpullse.com/actions/execute', {
    method: 'POST',
    headers: {
      'x-org-id': 'org_1234567890abcdef',
      'x-user-id': 'user_abcdef1234567890',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(request)
  });

  if (!response.ok) {
    throw new Error(`HTTP ${response.status}: ${response.statusText}`);
  }

  return response.json();
};

// Send welcome email
try {
  const result = await executeAction({
    action: 'email-send',
    inputs: {
      to: 'customer@example.com',
      subject: 'Welcome!',
      template: 'welcome-template',
      variables: {
        customer_name: 'John Doe',
        plan_type: 'Premium'
      }
    },
    timeout_seconds: 60,
    tags: ['welcome-email', 'onboarding']
  });

  if (result.success && result.data.status === 'completed') {
    console.log('Email sent successfully:', result.data.outputs?.message_id);
  } else {
    console.error('Email failed:', result.data.error?.message);
  }
} catch (error) {
  console.error('Request failed:', error);
}
```

```python Python
import requests
import json
from typing import Dict, Any, Optional, List

class ActionExecutor:
    def __init__(self, org_id: str, user_id: str, base_url: str = "https://tolstoy.getpullse.com"):
        self.org_id = org_id
        self.user_id = user_id
        self.base_url = base_url
        self.headers = {
            'x-org-id': org_id,
            'x-user-id': user_id,
            'Content-Type': 'application/json'
        }

    def execute_action(
        self,
        action: str,
        inputs: Dict[str, Any],
        timeout_seconds: int = 30,
        tags: Optional[List[str]] = None,
        dry_run: bool = False,
        priority: str = 'normal'
    ) -> Dict[str, Any]:
        """Execute a single action with the given inputs."""
        
        payload = {
            'action': action,
            'inputs': inputs,
            'timeout_seconds': timeout_seconds,
            'priority': priority
        }
        
        if tags:
            payload['tags'] = tags
        if dry_run:
            payload['dry_run'] = dry_run

        response = requests.post(
            f"{self.base_url}/actions/execute",
            headers=self.headers,
            json=payload
        )
        response.raise_for_status()
        
        return response.json()

    def send_slack_message(self, channel: str, message: str, **kwargs) -> Dict[str, Any]:
        """Helper method to send Slack messages."""
        inputs = {
            'channel': channel,
            'message': message,
            **kwargs
        }
        
        return self.execute_action('slack-message', inputs, tags=['slack', 'notification'])

    def make_http_request(self, url: str, method: str = 'GET', **kwargs) -> Dict[str, Any]:
        """Helper method to make HTTP requests."""
        inputs = {
            'url': url,
            'method': method,
            **kwargs
        }
        
        return self.execute_action('http-request', inputs, tags=['http', 'api-call'])

    def validate_email(self, email: str) -> Dict[str, Any]:
        """Helper method to validate email addresses."""
        return self.execute_action('email-validate', {'email': email}, tags=['validation'])

# Usage examples
executor = ActionExecutor('org_1234567890abcdef', 'user_abcdef1234567890')

# Send an email
email_result = executor.execute_action(
    'email-send',
    {
        'to': 'customer@example.com',
        'subject': 'Order Confirmation',
        'template': 'order-confirmation',
        'variables': {
            'order_id': 'ORD-12345',
            'total_amount': '$99.99',
            'customer_name': 'John Doe'
        }
    },
    timeout_seconds=60,
    tags=['order-confirmation', 'automated']
)

if email_result['success']:
    print(f"Email sent! Message ID: {email_result['data']['outputs'].get('message_id')}")
else:
    print(f"Failed to send email: {email_result['data']['error']['message']}")

# Send Slack notification
slack_result = executor.send_slack_message(
    channel='#orders',
    message='🎉 New order received!',
    attachments=[{
        'color': 'good',
        'title': 'Order Details',
        'fields': [
            {'title': 'Order ID', 'value': 'ORD-12345', 'short': True},
            {'title': 'Amount', 'value': '$99.99', 'short': True}
        ]
    }]
)

# Make API call to update CRM
crm_result = executor.make_http_request(
    url='https://api.crm.com/customers',
    method='POST',
    headers={'Authorization': 'Bearer token'},
    body={
        'name': 'John Doe',
        'email': 'customer@example.com',
        'source': 'website'
    }
)
```

```javascript Node.js
const axios = require('axios');

class ActionExecutor {
  constructor(orgId, userId, baseUrl = 'https://tolstoy.getpullse.com') {
    this.orgId = orgId;
    this.userId = userId;
    this.baseUrl = baseUrl;
    this.headers = {
      'x-org-id': orgId,
      'x-user-id': userId,
      'Content-Type': 'application/json'
    };
  }

  async executeAction(action, inputs, options = {}) {
    const {
      timeout_seconds = 30,
      tags = [],
      dry_run = false,
      priority = 'normal'
    } = options;

    const payload = {
      action,
      inputs,
      timeout_seconds,
      tags,
      priority
    };

    if (dry_run) {
      payload.dry_run = true;
    }

    try {
      const response = await axios.post(
        `${this.baseUrl}/actions/execute`,
        payload,
        { headers: this.headers }
      );

      return response.data;
    } catch (error) {
      if (error.response) {
        throw new Error(`API Error: ${error.response.status} - ${error.response.data?.error?.message || error.message}`);
      }
      throw error;
    }
  }

  // Helper methods for common actions
  async sendEmail(to, subject, body, options = {}) {
    return this.executeAction('email-send', {
      to,
      subject,
      body,
      ...options
    }, { tags: ['email', 'notification'] });
  }

  async sendSlackMessage(channel, message, options = {}) {
    return this.executeAction('slack-message', {
      channel,
      message,
      ...options
    }, { tags: ['slack', 'notification'] });
  }

  async makeHttpRequest(url, method = 'GET', options = {}) {
    return this.executeAction('http-request', {
      url,
      method,
      ...options
    }, { tags: ['http', 'api-call'] });
  }

  async transformData(data, transformRules) {
    return this.executeAction('data-transform', {
      data,
      rules: transformRules
    }, { tags: ['data', 'transformation'] });
  }

  async validateData(data, schema) {
    return this.executeAction('data-validate', {
      data,
      schema
    }, { tags: ['validation'] });
  }
}

// Usage examples
const executor = new ActionExecutor('org_1234567890abcdef', 'user_abcdef1234567890');

async function processNewCustomer(customerData) {
  try {
    // Validate customer data
    const validation = await executor.validateData(customerData, {
      type: 'object',
      required: ['email', 'name'],
      properties: {
        email: { type: 'string', format: 'email' },
        name: { type: 'string', minLength: 1 }
      }
    });

    if (validation.data.status !== 'completed' || !validation.data.outputs.valid) {
      throw new Error('Customer data validation failed');
    }

    // Send welcome email
    const emailResult = await executor.sendEmail(
      customerData.email,
      'Welcome to our platform!',
      null, // Will use template instead of body
      {
        template: 'welcome-template',
        variables: {
          customer_name: customerData.name,
          login_url: 'https://app.example.com/login'
        }
      }
    );

    // Notify team on Slack
    const slackResult = await executor.sendSlackMessage(
      '#new-customers',
      `🎉 New customer signed up: ${customerData.name}`,
      {
        attachments: [{
          color: 'good',
          fields: [
            { title: 'Name', value: customerData.name, short: true },
            { title: 'Email', value: customerData.email, short: true }
          ]
        }]
      }
    );

    // Update CRM
    const crmResult = await executor.makeHttpRequest(
      'https://api.crm.com/contacts',
      'POST',
      {
        headers: { 'Authorization': 'Bearer crm-token' },
        body: {
          email: customerData.email,
          name: customerData.name,
          source: 'website_signup',
          tags: ['new_customer']
        }
      }
    );

    console.log('Customer onboarding completed successfully');
    return {
      email_sent: emailResult.success,
      slack_notified: slackResult.success,
      crm_updated: crmResult.success
    };

  } catch (error) {
    console.error('Customer onboarding failed:', error.message);
    throw error;
  }
}

// Process a new customer
processNewCustomer({
  name: 'Jane Smith',
  email: 'jane@example.com',
  plan: 'premium'
}).then(result => {
  console.log('Onboarding result:', result);
}).catch(error => {
  console.error('Error:', error.message);
});
```

</RequestExample>

## Request Body

<ParamField body="action" type="string" required>
The action identifier to execute. Must match an action available in your organization.

**Format:** Action IDs are kebab-case strings (e.g., `email-send`, `slack-message`, `http-request`)

**Examples:**
- `email-send` - Send email messages
- `slack-message` - Send Slack notifications  
- `http-request` - Make HTTP API calls
- `data-transform` - Transform data structures
</ParamField>

<ParamField body="inputs" type="object" required>
Input parameters for the action execution. Must conform to the action's input schema.

**Format:** Key-value pairs where keys match the action's expected input parameter names.

**Validation:** Inputs are validated against the action's JSON schema before execution.
</ParamField>

<ParamField body="timeout_seconds" type="integer" default="30">
Maximum execution time in seconds before the action times out.

**Constraints:**
- Minimum: 1 second
- Maximum: 300 seconds (5 minutes)
- Different actions may have stricter limits
</ParamField>

<ParamField body="tags" type="string[]">
Optional tags to categorize this execution for tracking and analytics.

**Constraints:**
- Maximum 10 tags per execution
- Each tag: 1-50 characters
- Tags are case-insensitive
</ParamField>

<ParamField body="dry_run" type="boolean" default="false">
When true, validates inputs and returns execution plan without actually running the action.

**Use cases:**
- Testing action configurations
- Validating input data
- Debugging integration issues
</ParamField>

<ParamField body="priority" type="string" default="normal">
Execution priority level for queue management.

**Available priorities:**
- `low` - Lower priority (may wait longer in queue)
- `normal` - Standard priority
- `high` - Higher priority (faster processing)
</ParamField>

<ParamField body="context" type="object">
Optional execution context for advanced scenarios.

<Expandable title="Context Fields">
  <ParamField body="user_context" type="object">
    User-specific context data available to the action
  </ParamField>
  <ParamField body="organization_context" type="object">
    Organization-specific context data
  </ParamField>
  <ParamField body="trace_id" type="string">
    Custom trace ID for request correlation
  </ParamField>
</Expandable>
</ParamField>

## Response

<ResponseExample>

```json Success Response - Email Action
{
  "success": true,
  "data": {
    "execution_id": "exec_action_abc123def456",
    "action": "email-send",
    "status": "completed",
    "inputs": {
      "to": "customer@example.com",
      "subject": "Welcome to our platform!",
      "template": "welcome-template",
      "variables": {
        "customer_name": "John Doe",
        "plan_type": "Premium"
      }
    },
    "outputs": {
      "message_id": "msg_xyz789abc123",
      "sent_at": "2024-01-15T14:30:15Z",
      "status": "sent",
      "provider": "sendgrid",
      "recipient": "customer@example.com",
      "subject": "Welcome to our platform!",
      "tracking": {
        "opens": 0,
        "clicks": 0,
        "tracking_enabled": true
      }
    },
    "performance": {
      "duration_ms": 1245,
      "memory_used_mb": 12,
      "network_requests": 1,
      "queue_wait_ms": 45
    },
    "executed_at": "2024-01-15T14:30:14Z",
    "completed_at": "2024-01-15T14:30:15Z",
    "tags": ["welcome-email", "onboarding"]
  },
  "metadata": {
    "request_id": "req_abc123def456",
    "timestamp": "2024-01-15T14:30:15Z",
    "action_version": "2.1.0"
  }
}
```

```json Success Response - HTTP Action
{
  "success": true,
  "data": {
    "execution_id": "exec_action_http456def789",
    "action": "http-request",
    "status": "completed",
    "inputs": {
      "url": "https://api.example.com/customers",
      "method": "POST",
      "headers": {
        "Authorization": "Bearer token",
        "Content-Type": "application/json"
      },
      "body": {
        "name": "John Doe",
        "email": "john@example.com",
        "plan": "premium"
      }
    },
    "outputs": {
      "status_code": 201,
      "headers": {
        "Content-Type": "application/json",
        "Location": "/customers/cust_12345"
      },
      "body": {
        "id": "cust_12345",
        "name": "John Doe",
        "email": "john@example.com",
        "plan": "premium",
        "created_at": "2024-01-15T14:30:16Z"
      },
      "duration_ms": 342,
      "success": true
    },
    "performance": {
      "duration_ms": 358,
      "memory_used_mb": 8,
      "network_requests": 1
    },
    "executed_at": "2024-01-15T14:30:15Z",
    "completed_at": "2024-01-15T14:30:16Z"
  },
  "metadata": {
    "request_id": "req_http123",
    "timestamp": "2024-01-15T14:30:16Z",
    "action_version": "3.2.0"
  }
}
```

```json Success Response - Slack Action
{
  "success": true,
  "data": {
    "execution_id": "exec_action_slack789abc123",
    "action": "slack-message",
    "status": "completed",
    "inputs": {
      "channel": "#alerts",
      "message": "System Alert: High CPU Usage",
      "attachments": [
        {
          "color": "danger",
          "title": "Server Alert",
          "text": "CPU usage is at 95% on web-01",
          "fields": [
            {
              "title": "Server",
              "value": "web-01",
              "short": true
            },
            {
              "title": "CPU Usage",
              "value": "95%",
              "short": true
            }
          ]
        }
      ]
    },
    "outputs": {
      "ts": "1705330816.123456",
      "channel": "C1234567890",
      "permalink": "https://workspace.slack.com/archives/C1234567890/p1705330816123456",
      "message": {
        "type": "message",
        "user": "U0BOTUSER",
        "text": "System Alert: High CPU Usage",
        "attachments": [...],
        "ts": "1705330816.123456"
      }
    },
    "performance": {
      "duration_ms": 523,
      "memory_used_mb": 6,
      "network_requests": 1
    },
    "executed_at": "2024-01-15T14:30:16Z",
    "completed_at": "2024-01-15T14:30:16Z",
    "tags": ["alert", "monitoring"]
  },
  "metadata": {
    "request_id": "req_slack456",
    "timestamp": "2024-01-15T14:30:16Z",
    "action_version": "1.8.0"
  }
}
```

```json Action Failure Response
{
  "success": true,
  "data": {
    "execution_id": "exec_action_failed123",
    "action": "email-send",
    "status": "failed",
    "inputs": {
      "to": "invalid-email-address",
      "subject": "Test Email",
      "body": "This is a test."
    },
    "error": {
      "code": "INVALID_EMAIL_ADDRESS",
      "message": "The provided email address is not valid",
      "details": {
        "field": "to",
        "value": "invalid-email-address",
        "reason": "Email format validation failed",
        "suggestions": [
          "Check for typos in the email address",
          "Ensure the email follows the format: user@domain.com"
        ]
      },
      "retry_after": null,
      "is_retryable": false
    },
    "performance": {
      "duration_ms": 45,
      "memory_used_mb": 3
    },
    "executed_at": "2024-01-15T14:30:17Z",
    "failed_at": "2024-01-15T14:30:17Z"
  },
  "metadata": {
    "request_id": "req_failed789",
    "timestamp": "2024-01-15T14:30:17Z"
  }
}
```

```json Timeout Response
{
  "success": true,
  "data": {
    "execution_id": "exec_action_timeout456",
    "action": "http-request",
    "status": "timeout",
    "inputs": {
      "url": "https://slow-api.example.com/data",
      "method": "GET",
      "timeout_seconds": 5
    },
    "error": {
      "code": "EXECUTION_TIMEOUT",
      "message": "Action execution exceeded the specified timeout",
      "details": {
        "timeout_seconds": 5,
        "actual_duration_ms": 5000
      },
      "retry_after": 60,
      "is_retryable": true
    },
    "performance": {
      "duration_ms": 5000,
      "memory_used_mb": 5
    },
    "executed_at": "2024-01-15T14:30:20Z",
    "timeout_at": "2024-01-15T14:30:25Z"
  },
  "metadata": {
    "request_id": "req_timeout123",
    "timestamp": "2024-01-15T14:30:25Z"
  }
}
```

```json Dry Run Response
{
  "success": true,
  "data": {
    "dry_run": true,
    "action": "email-send",
    "validation": {
      "inputs_valid": true,
      "required_fields_present": true,
      "schema_valid": true,
      "auth_configured": true
    },
    "execution_plan": {
      "action_version": "2.1.0",
      "provider": "sendgrid",
      "estimated_duration_ms": 1200,
      "resolved_inputs": {
        "to": "customer@example.com",
        "subject": "Welcome to our platform!",
        "from": "noreply@company.com",
        "template_id": "template_12345",
        "resolved_variables": {
          "customer_name": "John Doe",
          "plan_type": "Premium"
        }
      },
      "dependencies_check": {
        "auth_provider": "configured",
        "template_exists": true,
        "rate_limits": {
          "current_usage": 45,
          "limit": 100,
          "period": "minute"
        }
      }
    },
    "warnings": [],
    "estimated_cost": {
      "credits": 1,
      "currency": "USD",
      "amount": 0.001
    }
  },
  "metadata": {
    "request_id": "req_dryrun123",
    "timestamp": "2024-01-15T14:30:25Z"
  }
}
```

```json Input Validation Error
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Action input validation failed",
    "details": {
      "action": "email-send",
      "field": "to",
      "value": null,
      "reason": "Required field 'to' is missing",
      "schema": {
        "type": "string",
        "format": "email",
        "required": true
      }
    }
  },
  "metadata": {
    "request_id": "req_validation_error",
    "timestamp": "2024-01-15T14:30:26Z"
  }
}
```

```json Action Not Found Error
{
  "success": false,
  "error": {
    "code": "ACTION_NOT_FOUND",
    "message": "The specified action was not found or is not available",
    "details": {
      "action": "nonexistent-action",
      "available_actions": [
        "email-send",
        "slack-message", 
        "http-request",
        "data-transform"
      ]
    }
  },
  "metadata": {
    "request_id": "req_not_found",
    "timestamp": "2024-01-15T14:30:27Z"
  }
}
```

</ResponseExample>

## Response Fields

<ResponseField name="execution_id" type="string">
Unique identifier for this action execution
</ResponseField>

<ResponseField name="action" type="string">
The action that was executed
</ResponseField>

<ResponseField name="status" type="string">
Execution status:
- `completed` - Action executed successfully
- `failed` - Action failed with error
- `timeout` - Action exceeded timeout limit
</ResponseField>

<ResponseField name="inputs" type="object">
The input parameters that were provided for execution
</ResponseField>

<ResponseField name="outputs" type="object">
Results returned by the action (only present when status is `completed`)
</ResponseField>

<ResponseField name="error" type="object">
Error information when execution fails
  <Expandable title="Error Fields">
    <ResponseField name="code" type="string">Error code identifying the type of failure</ResponseField>
    <ResponseField name="message" type="string">Human-readable error description</ResponseField>
    <ResponseField name="details" type="object">Additional error context and debugging information</ResponseField>
    <ResponseField name="is_retryable" type="boolean">Whether this error can be resolved by retrying</ResponseField>
    <ResponseField name="retry_after" type="integer">Recommended retry delay in seconds (if retryable)</ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="performance" type="object">
Execution performance metrics
  <Expandable title="Performance Fields">
    <ResponseField name="duration_ms" type="integer">Total execution time in milliseconds</ResponseField>
    <ResponseField name="memory_used_mb" type="number">Peak memory usage in megabytes</ResponseField>
    <ResponseField name="network_requests" type="integer">Number of external network requests made</ResponseField>
    <ResponseField name="queue_wait_ms" type="integer">Time spent waiting in execution queue</ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="executed_at" type="string">
ISO 8601 timestamp when execution started
</ResponseField>

<ResponseField name="completed_at" type="string">
ISO 8601 timestamp when execution completed successfully
</ResponseField>

<ResponseField name="failed_at" type="string">
ISO 8601 timestamp when execution failed
</ResponseField>

<ResponseField name="timeout_at" type="string">
ISO 8601 timestamp when execution timed out
</ResponseField>

<ResponseField name="tags" type="string[]">
Tags associated with this execution
</ResponseField>

## Status Codes

<ResponseField name="200" type="Success">
Action executed successfully (regardless of action success/failure)
</ResponseField>

<ResponseField name="400" type="Bad Request">
Invalid request format or missing required fields
</ResponseField>

<ResponseField name="401" type="Unauthorized">
Missing or invalid authentication headers
</ResponseField>

<ResponseField name="403" type="Forbidden">
Insufficient permissions to execute actions
</ResponseField>

<ResponseField name="404" type="Not Found">
Specified action not found or not available
</ResponseField>

<ResponseField name="422" type="Unprocessable Entity">
Action input validation failed
</ResponseField>

<ResponseField name="429" type="Too Many Requests">
Rate limit exceeded or execution queue full
</ResponseField>

<ResponseField name="500" type="Internal Server Error">
Server error during action execution
</ResponseField>

## Common Actions

### Email Actions

<CodeGroup>

```json Send Basic Email
{
  "action": "email-send",
  "inputs": {
    "to": "recipient@example.com",
    "subject": "Subject Line",
    "body": "Email body content",
    "from": "sender@company.com"
  }
}
```

```json Send Template Email
{
  "action": "email-send", 
  "inputs": {
    "to": "customer@example.com",
    "template": "welcome-template",
    "variables": {
      "customer_name": "John Doe",
      "activation_link": "https://app.com/activate/token123"
    }
  }
}
```

```json Send Email with Attachments
{
  "action": "email-send",
  "inputs": {
    "to": "customer@example.com",
    "subject": "Your Receipt",
    "body": "Please find your receipt attached.",
    "attachments": [
      {
        "filename": "receipt.pdf",
        "url": "https://storage.com/receipts/receipt-123.pdf",
        "content_type": "application/pdf"
      }
    ]
  }
}
```

</CodeGroup>

### Slack Actions

<CodeGroup>

```json Simple Message
{
  "action": "slack-message",
  "inputs": {
    "channel": "#general",
    "message": "Hello team! 👋"
  }
}
```

```json Rich Message with Attachments
{
  "action": "slack-message",
  "inputs": {
    "channel": "#alerts",
    "message": "System Alert",
    "username": "AlertBot",
    "icon_emoji": ":warning:",
    "attachments": [
      {
        "color": "danger",
        "title": "High CPU Usage",
        "text": "CPU usage is at 95%",
        "fields": [
          {
            "title": "Server",
            "value": "web-01",
            "short": true
          },
          {
            "title": "Usage",
            "value": "95%",
            "short": true
          }
        ]
      }
    ]
  }
}
```

```json Direct Message
{
  "action": "slack-message",
  "inputs": {
    "channel": "@john.doe",
    "message": "Hi John, can you review this when you have a moment?",
    "attachments": [
      {
        "title": "Pull Request #123",
        "title_link": "https://github.com/company/repo/pull/123",
        "text": "Add new customer dashboard feature"
      }
    ]
  }
}
```

</CodeGroup>

### HTTP Request Actions

<CodeGroup>

```json GET Request with Query Params
{
  "action": "http-request",
  "inputs": {
    "url": "https://api.example.com/users",
    "method": "GET",
    "query_params": {
      "page": 1,
      "limit": 20,
      "status": "active"
    },
    "headers": {
      "Authorization": "Bearer token123",
      "User-Agent": "MyApp/1.0"
    }
  }
}
```

```json POST Request with JSON Body
{
  "action": "http-request",
  "inputs": {
    "url": "https://api.crm.com/contacts",
    "method": "POST",
    "headers": {
      "Authorization": "Bearer crm-token",
      "Content-Type": "application/json"
    },
    "body": {
      "name": "Jane Smith",
      "email": "jane@example.com",
      "company": "Acme Corp"
    },
    "timeout_seconds": 30
  }
}
```

```json Webhook with Retry Configuration
{
  "action": "http-request",
  "inputs": {
    "url": "https://webhook.site/unique-id",
    "method": "POST",
    "headers": {
      "Content-Type": "application/json",
      "X-Webhook-Source": "tolstoy"
    },
    "body": {
      "event": "user_signup",
      "data": {
        "user_id": "12345",
        "email": "user@example.com",
        "timestamp": "2024-01-15T14:30:00Z"
      }
    },
    "retry_config": {
      "max_attempts": 3,
      "delay_ms": 2000,
      "backoff_multiplier": 2.0
    }
  }
}
```

</CodeGroup>

### Data Transformation Actions

<CodeGroup>

```json Transform JSON Data
{
  "action": "data-transform",
  "inputs": {
    "data": {
      "user": {
        "first_name": "John",
        "last_name": "Doe",
        "contact": {
          "email": "john@example.com",
          "phone": "+1234567890"
        }
      }
    },
    "rules": [
      {
        "operation": "flatten",
        "path": "user.contact",
        "prefix": "contact_"
      },
      {
        "operation": "combine",
        "fields": ["user.first_name", "user.last_name"],
        "output": "full_name",
        "separator": " "
      }
    ]
  }
}
```

```json Validate Data Schema
{
  "action": "data-validate",
  "inputs": {
    "data": {
      "email": "user@example.com",
      "age": 25,
      "preferences": {
        "notifications": true,
        "theme": "dark"
      }
    },
    "schema": {
      "type": "object",
      "required": ["email", "age"],
      "properties": {
        "email": {
          "type": "string",
          "format": "email"
        },
        "age": {
          "type": "integer",
          "minimum": 18
        },
        "preferences": {
          "type": "object",
          "properties": {
            "notifications": {"type": "boolean"},
            "theme": {"type": "string", "enum": ["light", "dark"]}
          }
        }
      }
    }
  }
}
```

</CodeGroup>

## Error Handling

### Common Error Patterns

<Accordion title="Input Validation Errors">
These occur when the provided inputs don't match the action's schema:

```json
{
  "code": "VALIDATION_ERROR",
  "message": "Input validation failed",
  "details": {
    "field": "email",
    "value": "invalid-email",
    "reason": "Must be a valid email address"
  }
}
```

**Resolution:** Check action documentation for correct input format and validate data before sending.
</Accordion>

<Accordion title="Authentication Errors">
These occur when the action requires authentication that isn't properly configured:

```json
{
  "code": "AUTH_ERROR",
  "message": "Authentication failed for action provider",
  "details": {
    "provider": "slack",
    "auth_type": "oauth2",
    "error": "Invalid or expired token"
  }
}
```

**Resolution:** Check your integrations and refresh authentication tokens if needed.
</Accordion>

<Accordion title="Rate Limit Errors">
These occur when you exceed the action's rate limits:

```json
{
  "code": "RATE_LIMITED",
  "message": "Action rate limit exceeded",
  "details": {
    "limit": 100,
    "period": "minute", 
    "retry_after": 45
  }
}
```

**Resolution:** Implement exponential backoff and respect the retry_after value.
</Accordion>

<Accordion title="Provider Errors">
These are errors from external services that the action integrates with:

```json
{
  "code": "PROVIDER_ERROR", 
  "message": "External service returned an error",
  "details": {
    "provider": "sendgrid",
    "provider_code": "400",
    "provider_message": "The from email does not match a verified Sender Identity"
  }
}
```

**Resolution:** Check the provider's documentation and fix the configuration issue.
</Accordion>

## Best Practices

### Input Handling

<Accordion title="Data Validation">
- Always validate inputs client-side before sending requests
- Use dry_run mode to test configurations before production use
- Handle edge cases like null values, empty strings, and special characters
- Consider data size limits (typically 10MB max per execution)
</Accordion>

<Accordion title="Sensitive Data">
- Never include secrets or credentials directly in inputs
- Use secret management features or environment variables
- Be careful with logging and error messages that might expose sensitive data
- Consider using data masking for PII in logs
</Accordion>

### Performance Optimization

<Accordion title="Timeout Management">
- Set appropriate timeouts based on expected action duration
- Consider network latency for HTTP-based actions
- Use shorter timeouts for time-sensitive operations
- Monitor timeout patterns to optimize default values
</Accordion>

<Accordion title="Error Recovery">
- Implement retry logic for transient failures
- Use exponential backoff for rate-limited actions
- Have fallback strategies for critical operations
- Monitor error rates to identify systemic issues
</Accordion>

### Testing and Debugging

<Accordion title="Dry Run Testing">
- Use dry_run mode to validate configurations
- Test with various input scenarios
- Verify authentication and permissions
- Check estimated costs and resource usage
</Accordion>

<Accordion title="Monitoring">
- Use tags consistently for execution tracking
- Monitor performance metrics and success rates
- Set up alerts for high error rates
- Review execution logs regularly
</Accordion>

## Related Endpoints

<CardGroup cols={2}>
  <Card title="List Actions" icon="list" href="/api/actions/get-actions">
    Browse available actions and their schemas
  </Card>
  <Card title="Execute in Flow" icon="workflow" href="/api/flows/post-flows-execute">
    Execute actions as part of workflow orchestration
  </Card>
</CardGroup>

---

<Info>
**Tip**: Use the dry_run parameter to validate your action configuration and inputs before actual execution. This helps catch configuration issues early and prevents unnecessary API calls.
</Info>
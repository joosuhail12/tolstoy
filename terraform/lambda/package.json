{
  "name": "tolstoy-backup-lambda",
  "version": "1.0.0",
  "description": "Automated Neon PostgreSQL backup to S3 for Tolstoy",
  "main": "backup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "postgresql",
    "backup",
    "neon",
    "aws",
    "lambda",
    "s3"
  ],
  "author": "Tolstoy Team",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-secrets-manager": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
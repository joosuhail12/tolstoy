---
title: "Get Tool"
api: "GET /tools/{id}"
description: "Get a specific tool by ID"
---

# Get Tool

Get a specific tool by ID

## Authentication

<ParamField header="x-org-id" type="string" required>
  Your organization ID
</ParamField>

<ParamField header="x-user-id" type="string" required>
  Your user ID
</ParamField>

<ParamField path="id" type="string" required>
  Tool ID
</ParamField>



## Examples

<CodeGroup>
```bash cURL
curl -X GET "http://localhost:3000/tools/{id}" \
  -H "Content-Type: application/json" \
  -H "x-org-id: org_abc123def456" \
  -H "x-user-id: user_xyz789abc"
```

```javascript JavaScript SDK
import { TolstoyClient } from '@tolstoy/sdk';

const tolstoy = new TolstoyClient({
  orgId: 'org_abc123def456',
  userId: 'user_xyz789abc'
});

const result = await tolstoy.tools.toolscontroller_findone();
console.log(result);
```
</CodeGroup>

## Responses

<ResponseExample>
```json 200 OK
{
  "id": "tool_abc123",
  "name": "Slack Notifier",
  "type": "notification",
  "configuration": {
    "baseUrl": "https://hooks.slack.com"
  },
  "description": "Send notifications to Slack channels",
  "version": "1.0.0",
  "createdAt": "2024-01-15T10:30:00.000Z",
  "updatedAt": "2024-01-15T10:30:00.000Z"
}
```
</ResponseExample>

### 404 Not Found

Tool not found

---

<Snippet file="api-footer.mdx" />

---
title: "Create Tool"
api: "POST /tools"
description: "Register a new external tool for use in workflows"
---

# Create Tool

Register a new external tool for use in workflows

## Authentication

<ParamField header="x-org-id" type="string" required>
  Your organization ID
</ParamField>

<ParamField header="x-user-id" type="string" required>
  Your user ID
</ParamField>





## Examples

<CodeGroup>
```bash cURL
curl -X POST "http://localhost:3000/tools" \
  -H "Content-Type: application/json" \
  -H "x-org-id: org_abc123def456" \
  -H "x-user-id: user_xyz789abc" \
  -d '{}'
```

```javascript JavaScript SDK
import { TolstoyClient } from '@tolstoy/sdk';

const tolstoy = new TolstoyClient({
  orgId: 'org_abc123def456',
  userId: 'user_xyz789abc'
});

const result = await tolstoy.tools.toolscontroller_create();
console.log(result);
```
</CodeGroup>

## Responses

<ResponseExample>
```json 201 Created
{
  "id": "cme3zjbwc0000uppplgvx1hse",
  "name": "Slack Notifier",
  "baseUrl": "https://hooks.slack.com/services",
  "authType": "apiKey",
  "orgId": "org_123",
  "createdAt": "2024-01-15T10:30:00.000Z",
  "updatedAt": "2024-01-15T10:30:00.000Z"
}
```
</ResponseExample>

### 400 Bad Request

Invalid tool configuration

---

<Snippet file="api-footer.mdx" />

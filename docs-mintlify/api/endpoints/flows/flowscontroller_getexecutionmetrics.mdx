---
title: "Get Execution Metrics"
api: "GET /flows/{id}/metrics"
description: "Get execution metrics and statistics for a flow"
---

# Get Execution Metrics

Get execution metrics and statistics for a flow

## Authentication

<ParamField header="x-org-id" type="string" required>
  Your organization ID
</ParamField>

<ParamField header="x-user-id" type="string" required>
  Your user ID
</ParamField>

<ParamField path="id" type="string" required>
  Flow ID
</ParamField>



## Examples

<CodeGroup>
```bash cURL
curl -X GET "http://localhost:3000/flows/{id}/metrics" \
  -H "Content-Type: application/json" \
  -H "x-org-id: org_abc123def456" \
  -H "x-user-id: user_xyz789abc"
```

```javascript JavaScript SDK
import { TolstoyClient } from '@tolstoy/sdk';

const tolstoy = new TolstoyClient({
  orgId: 'org_abc123def456',
  userId: 'user_xyz789abc'
});

const result = await tolstoy.flows.flowscontroller_getexecutionmetrics();
console.log(result);
```
</CodeGroup>

## Responses

<ResponseExample>
```json 200 OK
{
  "totalExecutions": 152,
  "successfulExecutions": 140,
  "failedExecutions": 12,
  "averageExecutionTime": 4250.5,
  "lastExecutionAt": "2024-01-15T10:30:00.000Z",
  "successRate": 0.921
}
```
</ResponseExample>

### 404 Not Found

Flow not found

---

<Snippet file="api-footer.mdx" />

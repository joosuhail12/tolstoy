---
title: "Get Action"
api: "GET /actions/{id}"
description: "Get a specific action template by ID"
---

# Get Action

Get a specific action template by ID

## Authentication

<ParamField header="x-org-id" type="string" required>
  Your organization ID
</ParamField>

<ParamField header="x-user-id" type="string" required>
  Your user ID
</ParamField>

<ParamField path="id" type="string" required>
  Action ID
</ParamField>



## Examples

<CodeGroup>
```bash cURL
curl -X GET "http://localhost:3000/actions/{id}" \
  -H "Content-Type: application/json" \
  -H "x-org-id: org_abc123def456" \
  -H "x-user-id: user_xyz789abc"
```

```javascript JavaScript SDK
import { TolstoyClient } from '@tolstoy/sdk';

const tolstoy = new TolstoyClient({
  orgId: 'org_abc123def456',
  userId: 'user_xyz789abc'
});

const result = await tolstoy.actions.actionscontroller_findone();
console.log(result);
```
</CodeGroup>

## Responses

<ResponseExample>
```json 200 OK
{
  "id": "action_abc123",
  "name": "Send Slack Message",
  "key": "slack_send_message",
  "toolId": "tool_slack_123",
  "method": "POST",
  "endpoint": "/api/chat.postMessage",
  "headers": {
    "Content-Type": "application/json",
    "Authorization": "Bearer {token}"
  },
  "inputSchema": [
    {
      "name": "channel",
      "type": "string",
      "required": true,
      "description": "Slack channel ID"
    },
    {
      "name": "text",
      "type": "string",
      "required": true,
      "description": "Message content"
    }
  ],
  "executeIf": {
    "user.role": "admin"
  },
  "version": 1,
  "createdAt": "2024-01-15T10:30:00.000Z",
  "updatedAt": "2024-01-15T10:30:00.000Z"
}
```
</ResponseExample>

### 404 Not Found

Action not found

---

<Snippet file="api-footer.mdx" />

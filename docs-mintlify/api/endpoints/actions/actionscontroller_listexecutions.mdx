---
title: "List Action Executions"
api: "GET /actions/executions"
description: "Get a list of all action executions for the organization"
---

# List Action Executions

Get a list of all action executions for the organization

## Authentication

<ParamField header="x-org-id" type="string" required>
  Your organization ID
</ParamField>

<ParamField header="x-user-id" type="string" required>
  Your user ID
</ParamField>

<ParamField header="X-Action-Key" type="string" required>
  
</ParamField>

<ParamField header="X-Status" type="string" required>
  
</ParamField>



## Examples

<CodeGroup>
```bash cURL
curl -X GET "http://localhost:3000/actions/executions" \
  -H "Content-Type: application/json" \
  -H "x-org-id: org_abc123def456" \
  -H "x-user-id: user_xyz789abc"
```

```javascript JavaScript SDK
import { TolstoyClient } from '@tolstoy/sdk';

const tolstoy = new TolstoyClient({
  orgId: 'org_abc123def456',
  userId: 'user_xyz789abc'
});

const result = await tolstoy.actions.actionscontroller_listexecutions();
console.log(result);
```
</CodeGroup>

## Responses

<ResponseExample>
```json 200 OK
[
  {}
]
```
</ResponseExample>

---

<Snippet file="api-footer.mdx" />

---
title: "Get Valid Event Types"
api: "GET /webhooks/event-types"
description: "Get list of all valid event types that can be subscribed to via webhooks"
---

# Get Valid Event Types

Get list of all valid event types that can be subscribed to via webhooks

## Authentication

<ParamField header="x-org-id" type="string" required>
  Your organization ID
</ParamField>

<ParamField header="x-user-id" type="string" required>
  Your user ID
</ParamField>





## Examples

<CodeGroup>
```bash cURL
curl -X GET "http://localhost:3000/webhooks/event-types" \
  -H "Content-Type: application/json" \
  -H "x-org-id: org_abc123def456" \
  -H "x-user-id: user_xyz789abc"
```

```javascript JavaScript SDK
import { TolstoyClient } from '@tolstoy/sdk';

const tolstoy = new TolstoyClient({
  orgId: 'org_abc123def456',
  userId: 'user_xyz789abc'
});

const result = await tolstoy.webhooks.webhookscontroller_getvalideventtypes();
console.log(result);
```
</CodeGroup>

## Responses

<ResponseExample>
```json 200 OK
{
  "eventTypes": [
    "flow.execution.started",
    "flow.execution.completed",
    "flow.execution.failed",
    "user.created",
    "organization.updated"
  ]
}
```
</ResponseExample>

---

<Snippet file="api-footer.mdx" />
